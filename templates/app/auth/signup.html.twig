{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1>Sign Up</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button class="btn btn-primary">Sign up</button>
    {{ form_end(form) }}
{% endblock %}
